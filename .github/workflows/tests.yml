on:
    pull_request: 
        branches:
            - "main"
            - "develop"

name: Tests

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Install asdf & tools
              uses: asdf-vm/actions/install@v4
