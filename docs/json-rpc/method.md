# Method

JSON-RPC methods in Artur define the remote procedure calls that can be invoked by clients. Each method represents a specific operation or function that can be executed on the server, complete with parameter handling, validation, and response formatting according to the JSON-RPC 2.0 specification.
