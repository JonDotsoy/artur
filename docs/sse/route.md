# Route

Server-Sent Events (SSE) routes in Artur enable real-time communication from server to client through persistent HTTP connections. SSE routes provide a simple and efficient way to stream live data, notifications, or updates to web clients without the complexity of WebSockets or polling mechanisms.
